# ⚠️ 测量专用分支,不可发布

本分支在 `hash_neon` 里**无条件**发 `VEOR3`(SHA3 扩展指令),用于量化收益。
在**没有 FEAT_SHA3 的 arm64 上会 SIGILL**(已在 qemu `-cpu neoverse-n1` 上复现)。

发布形态必须是「新后端 + 特性门」,不是改现有的 `hash_neon`。

## 改了什么

`ror(b^c, n) = ror(b,n) ^ (c>>n) ^ (c<<(32-n))` —— 旋转是位置换,对异或可分配。
把 `ror(b,n)` 提出 `c` 那条依赖链(它只依赖 `b`,早已就绪),`c` 的两半移位与三路异或用一条 `VEOR3` 完成。

`c` 到新 `b` 由 **3 层降到 2 层**,每个 mix step 的链 **7 → 6 层**(基线是 PR 48 之后的 7 层)。

## 指令账(闭合)

| | 改前 | 改后 |
|---|---|---|
| 总行数 | 4549 | 5445(**+896 = 448 站点 × 2**) |
| `VEOR3` | 0 | 448 |
| `VUSHR` | 0 | 448 |
| `VEOR` | 932 | 484(−448) |
| `VSHL` | 448 | 896(+448) |
| `VSRI` | 448 | 448 |

## 正确性

- qemu `-cpu neoverse-v1`(有 sha3、无 sve2 → 走 `hash_neon`):全量测试通过
- 长度 0..8192 穷举 + 分块写入交叉比对:通过
